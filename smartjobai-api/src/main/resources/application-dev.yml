spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartjobai?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    show-sql: true

app:
  jwt:
    # Só para desenvolvimento local — nunca reutilize este valor em produção.
    secret: dev-secret-troque-antes-de-ir-para-producao-0123456789

logging:
  level:
    com.smartjobai: DEBUG
    org.hibernate.SQL: DEBUG
